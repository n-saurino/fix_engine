#pragma once

/* NEXT IMPLEMENTATION */

// add a logger to save benchmark testing results to a csv
class PerformanceLogger
{
public:
    PerformanceLogger(/* args */);
    ~PerformanceLogger();

private:
    /* data */
};

